import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark', // Change to 'light' if you want light mode
    // You can customize further
  },
  // Add typography, components, etc. here
});

export default theme;
