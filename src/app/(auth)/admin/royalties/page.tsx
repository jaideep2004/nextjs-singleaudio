import PremiumPlaceholderPage from '@/components/PremiumPlaceholderPage';

export default function AdminRoyaltiesPage() {
  return (
    <PremiumPlaceholderPage
      eyebrow="Finance"
      title="Royalties"
      description="Review user royalty statements, track earnings, adjustments, and reconciliation workflows before payout approval."
    />
  );
}
