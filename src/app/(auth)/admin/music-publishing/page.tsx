import PremiumPlaceholderPage from '@/components/PremiumPlaceholderPage';

export default function AdminMusicPublishingPage() {
  return (
    <PremiumPlaceholderPage
      eyebrow="Admin Publishing"
      title="Music Publishing"
      description="Review publishing requests, registrations, and rights handoffs from the admin workspace."
    />
  );
}
