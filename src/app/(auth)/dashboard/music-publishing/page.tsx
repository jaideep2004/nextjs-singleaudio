import PremiumPlaceholderPage from '@/components/PremiumPlaceholderPage';

export default function MusicPublishingPage() {
  return (
    <PremiumPlaceholderPage
      eyebrow="Publishing"
      title="Music Publishing"
      description="Prepare publishing registrations, writer splits, and collection workflows from one clean workspace."
    />
  );
}
